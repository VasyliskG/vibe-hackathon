# 🚀 Stage 1 MVP: Система розподілу замовлень доставки

Node.js система для автоматичного призначення замовлень найближчим вільним кур'єрам.

## 📋 Вимоги Stage 1

### Вхідні дані

**Список кур'єрів:**
- `id` — унікальний ідентифікатор
- `status` — `Free` або `Busy`
- `координати` — `(x, y)` на сітці 0-100

**Нове замовлення:**
- `id` — унікальний ідентифікатор
- `координати ресторану` — `(x, y)` звідки забрати замовлення

### Логіка

1. Знайти всіх кур'єрів зі статусом `Free`
2. Обчислити відстань від кожного кур'єра до ресторану (Евклідова формула)
3. Призначити найближчого
4. Змінити його статус на `Busy`
5. Повернути результат у форматі JSON

### Формат відповіді

**Успішне призначення:**
```json
{
  "orderId": "order-1",
  "assignedCourierId": "courier-2",
  "distance": 7.07
}
```

**Немає вільних кур'єрів:**
```json
{
  "message": "No couriers available"
}
```

## 🏗️ Архітектура

```
src/
├── domain/              # Бізнес-сутності
│   ├── Location.js      # Координати (immutable, валідація 0-100)
│   ├── Order.js         # Замовлення з координатами ресторану
│   └── Courier.js       # Кур'єр зі статусом Free/Busy
├── services/            # Бізнес-логіка
│   └── AssignmentService.js  # Логіка призначення замовлень
├── utils/              # Допоміжні інструменти
│   └── DistanceCalculator.js # Евклідова відстань
└── index.js            # Демонстрація роботи системи
```

### Принципи

✅ **Чиста архітектура** — розділення відповідальності  
✅ **In-memory** — без бази даних  
✅ **Модульність** — кожен файл відповідає за одну річ  
✅ **CommonJS** — `require/module.exports`

## 🚀 Запуск

### Встановлення

```bash
# Клонувати репозиторій
git clone https://github.com/VasyliskG/vibe-hackathon.git
cd vibe-hackathon

# Запустити
npm start
```

### Розробка з автоперезапуском

```bash
npm run dev
```

## 📊 Приклад виводу

```
=== Stage 1 MVP: Система розподілу замовлень ===

📦 Список кур'єрів:
  Courier(courier-1, Location(10, 20), Free)
  Courier(courier-2, Location(50, 50), Free)
  Courier(courier-3, Location(80, 90), Busy)
  Courier(courier-4, Location(30, 40), Free)

📊 Початкова статистика: { total: 4, free: 3, busy: 1 }

🍕 Замовлення від ресторанів:
  Order(order-1, Restaurant: Location(15, 25), Assigned: None)
  Order(order-2, Restaurant: Location(55, 48), Assigned: None)
  Order(order-3, Restaurant: Location(28, 38), Assigned: None)
  Order(order-4, Restaurant: Location(70, 80), Assigned: None)

--- Призначення замовлень ---

✅ order-1 → courier-1 (відстань: 7.07)
✅ order-2 → courier-2 (відстань: 5.39)
✅ order-3 → courier-4 (відстань: 2.83)
❌ order-4: No couriers available

📊 Фінальна статистика: { total: 4, free: 0, busy: 4 }
```

## 🧪 Особливості реалізації

### Location (Immutable)
- Валідація координат 0-100
- Незмінні після створення
- `Object.freeze()` для гарантії immutability

### Courier (Free/Busy)
- Статус змінюється через методи `markAsBusy()` / `markAsFree()`
- Захист від подвійної зміни статусу
- Метод `isFree()` для перевірки доступності

### Order
- Координати **ресторану** (не клієнта)
- Зберігає ID призначеного кур'єра
- Захист від повторного призначення

### AssignmentService
- **Тільки логіка призначення** (Single Responsibility)
- Алгоритм: найближчий вільний кур'єр
- Повертає JSON згідно зі с��ецифікацією

### DistanceCalculator
- Статичний клас (не потрібно створювати екземпляр)
- Евклідова формула: `√((x₂-x₁)² + (y₂-y₁)²)`
- Валідація типів параметрів

## 📝 Вимоги Stage 1

- ✅ Розділення по файлах (domain, services, utils)
- ✅ Без бази даних (in-memory)
- ✅ Код не в одному файлі
- ✅ AssignmentService містить тільки логіку розподілу
- ✅ DistanceCalculator винесений окремо
- ✅ index.js демонструє роботу

## 🎯 Stage 1 Checklist

- [x] Сутності: Location, Order, Courier
- [x] Статус Free/Busy для кур'єрів
- [x] Координати ресторану в замовленні
- [x] Алгоритм найближчого кур'єра
- [x] JSON відповідь з orderId, assignedCourierId, distance
- [x] Обробка випадку "No couriers available"
- [x] Чиста архітектура
- [x] Демонстраційний приклад

## 📦 package.json

```json
{
  "name": "vibe-hackathon",
  "version": "1.0.0",
  "description": "Stage 1 MVP: Order assignment system",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "keywords": ["delivery", "assignment", "mvp"],
  "author": "",
  "license": "ISC"
}
```

## 🔮 Roadmap (Stage 2+)

- [ ] REST API (Express.js)
- [ ] Тести (Jest)
- [ ] Логування (Winston)
- [ ] Різні стратегії призначення
- [ ] Пріоритети замовлень
- [ ] Історія замовлень

## 📄 Ліцензія

ISC